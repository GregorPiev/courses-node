<h1>{{title}}</h1>
{{#if orders.length}}
  {{#each orders}}
    <div class="row">
        <div class="col s6 offset-s3">
            
        </div>
    </div>
  {{/each}}
{{else}}
   <p>Orders cart is empty</p>
{{/if}}