const path = require('path');
const fs = require('fs');

const p = path.join(
    process.mainModule.filename,
    'data',
    'card.json'
);

class Card {
    add() {

    }
    
    static async fetch() {
        
    }
}


module.exports = Card;